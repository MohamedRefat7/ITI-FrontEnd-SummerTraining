- colors gradiants 
Linear Gradients (goes down/up/left/right/diagonally) (to direction or angle, color stop, color stop)
Radial Gradients (defined by their center)
Conic Gradients (rotated around a center point)

- !important

- font face
    = ttf otf

-transation : property duration



============================================================


- animation
@keyframes : hold what styles the element will have at certain times. scenarios 
    = from 0% to 100%       5s / 100
animation-name : اسم الفريم
animation-duration : طول مدة التنفيذ
animation-delay : يبدأ بعد قد ايه
animation-iteration-count : number , infinte
animation-direction
    =normal - The animation is played as normal (forwards). This is default
    =reverse - The animation is played in reverse direction (backwards)
    =alternate - The animation is played forwards first, then backwards
    =alternate-reverse - The animation is played backwards first, then forwards
animation-timing-function
    =ease - Specifies an animation with a slow start, then fast, then end slowly (this is default)
    =linear - Specifies an animation with the same speed from start to end
    =ease-in - Specifies an animation with a slow start
    =ease-out - Specifies an animation with a slow end
    =ease-in-out - Specifies an animation with a slow start and end
    =cubic-bezier(n,n,n,n) - Lets you define your own values in a cubic-bezier function
animation-fill-mode
animation : shorthand property


-transformation 2D - 3D


=translate()
rotate()
scaleX()
scaleY()
scale()
skewX()
skewY()
skew()
matrix()

- Reading
= https://www.w3schools.com/css/css_website_layout.asp
= https://flexboxfroggy.com/
= https://www.w3schools.com/css/css3_2dtransforms.asp
= https://fonts.google.com/